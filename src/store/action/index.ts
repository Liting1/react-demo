export default {
    /**
     * 修改主题
     * @param setStore
     * @param theme
     */
    setTheme(setStore: any, theme: string):void {
        setStore({theme})
    },
    /**
     * 修改当前选中的组件
     * @param setStore
     * @param data
     */
    setActiveModule(setStore: any, data: object): void{

    }
}
